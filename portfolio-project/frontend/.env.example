# Leave blank to use the Vite dev proxy (see vite.config.js) which forwards
# /api requests to http://localhost:4000. Set this to your deployed API URL
# in production, e.g. https://api.yourdomain.com
VITE_API_URL=
